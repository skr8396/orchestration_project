name: 'orchestration_project'
version: '1.0.0'
config-version: 2

profile: 'orchestration_project'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  orchestration_project:
    example_model:
      +materialized: table
