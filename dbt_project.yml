name: 'orchestration_project'
version: '1.0.0'
config-version: 2
 
profile: 'orchestration_project'
 
model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
 
vars:
  tbl_name: 'customer'
 
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
 
models:
  orchestration_project:
    +transient: false
    example_model:
      +materialized: table
